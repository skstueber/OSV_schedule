Hi possible future worker:

The only error in this code comes from the Macros. The plan was to use Render to transfer the reformatted data from a computer without git or the local repo file into the website. 
This worked for a time, but as edits were made to suit the different needs, it stopped working. The macro ExportSchedule was the culprit. Sometimes it would work however. 
A possible reason for this is a different compiler of VBA from the old version of Excel (2018) and the new version of the app/version on the web. This difference might account for a lot of errors. 

In terms of this project for future use, since you might not want a webpage etc, the macro ExportSchedule is the most useful as it reformats the data as we want to release it. 
The code is not all lost, just needs fixing. There are plenty of comments on it as well. 

If you do still want to use a webpage, I suggest the free version of Render with uptime robot, and using the code (outside of the macros) verbatim. 
